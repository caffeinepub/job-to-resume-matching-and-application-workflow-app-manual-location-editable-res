## Job-to-resume matching and application workflow app (manual location + editable resume/cover letter)

To run app build and run docker image: `docker build -t app . docker run -it --network host app`
